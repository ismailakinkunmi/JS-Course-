const firstName = "Adekunle";
const lastname = "Ismail";
const job = "Software web developer";
const year = 2050;
const birthDate = 1986;

console.log(
  `My name is ${firstName} ${lastname} a ${job} of ${
    year - birthDate
  } years old!`
);

console.log(
  `Some developer uses backtick in place of string but I don't use it.`
);

console.log(`Using string
to write
multiline
with backtick`);

console.log(
  "Using string to write\n multiline\n with interpolation double string"
);
